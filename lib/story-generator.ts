export function generateStoryContent(title: string, description: string): string {
  return `
    <h1>${title}</h1>
    <p>${description}</p>
    <p>في عالم يغلفه الاغتراب، تتكشف طبقات النفس البشرية تحت وطأة الأسئلة الوجودية.</p>
    <p>يتحول كل حدث بسيط إلى رمزية عميقة، تعكس وحدة الإنسان في عالم صاخب.</p>
    <p>ومع كل خطوة، يغوص أعمق في متاهة الوعي، باحثًا عن لحظة صدق وسط الزيف.</p>
  `;
}
